@using System.Web.Optimization
@model AssetManagementUI.ViewModels.AssetSetups.AssetTypeViewModels
@{
    ViewBag.Title = "AssetTypeForm";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="col-md-offset-1 col-sm-offset-1 col-md-10 col-sm-10">
    <div class="col-md-8 col-sm-8 noPadingLeft">
        <h2>New Asset Type</h2>
    </div>

    <div class="col-md-4 col-sm-4">
        <p>@Html.ActionLink("Back To List", "Index", "AssetTypes", null, new { @class = "btn btn-default align-right" })</p>

    </div>
</div>


@using (Html.BeginForm("Save", "AssetTypes"))
{
    <div id="saveForm" class="col-md-offset-1 col-sm-offset-1 col-md-6 col-sm-8">
        <div class="form-group">
            @Html.LabelFor(c => c.AssetType.Name)
            @Html.TextBoxFor(c => c.AssetType.Name, new { @class = "form-control" })
            @Html.ValidationMessageFor(c => c.AssetType.Name)
        </div>

        <div class="form-group">
            @Html.LabelFor(c => c.AssetType.ShortName)
            @Html.TextBoxFor(c => c.AssetType.ShortName, new { @class = "form-control" })
            @Html.ValidationMessageFor(c => c.AssetType.ShortName)
        </div>

        <div class="form-group">
            @Html.LabelFor(c => c.AssetType.Code)
            @Html.TextBoxFor(c => c.AssetType.Code, new { @class = "form-control" })
            @Html.ValidationMessageFor(c => c.AssetType.Code)
        </div>

        
        <div class="form-group">
            @Html.HiddenFor(c => c.AssetType.Id)
            @Html.AntiForgeryToken()
            <button id="saveButton" type="submit" class="btn btn-primary">Save</button>
        </div>

        <div class="@ViewBag.CssClass" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
            <strong>@ViewBag.MessageType</strong>@ViewBag.Message
        </div>
    </div>
}

@section scripts
{
    @Scripts.Render("~/bundles/jqueryval")
    <script src="~/Scripts/FormScript/assetType.js"></script>

}

