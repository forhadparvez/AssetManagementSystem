@using System.Web.Optimization
@model AssetManagementUI.ViewModels.BranchViewModel
@{
    ViewBag.Title = "BranchForm";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="col-md-offset-1 col-sm-offset-1 col-md-10 col-sm-10">
    <div class="col-md-8 col-sm-8 noPadingLeft">
        <h2>New Branch</h2>
    </div>

    <div class="col-md-4 col-sm-4">
        <p>@Html.ActionLink("Back To List", "Index", "Branchs", null, new { @class = "btn btn-default align-right" })</p>

    </div>
</div>


@using (Html.BeginForm("Save", "Branchs"))
{
    <div id="saveForm" class="col-md-offset-1 col-sm-offset-1 col-md-6 col-sm-8">
        <div class="form-group">
            @Html.LabelFor(c => c.Branch.OrganizationId)
            @Html.DropDownListFor(c=>c.Branch.OrganizationId, new SelectList(Model.Organizations, "Id", "Name"), "", new {@class="form-control"})
            @Html.ValidationMessageFor(c => c.Branch.OrganizationId)
        </div>
        <div class="form-group">
            @Html.LabelFor(c => c.Branch.Name)
            @Html.TextBoxFor(c => c.Branch.Name, new { @class = "form-control" })
            @Html.ValidationMessageFor(c => c.Branch.Name)
        </div>

        <div class="form-group">
            @Html.LabelFor(c => c.Branch.ShortName)
            @Html.TextBoxFor(c => c.Branch.ShortName, new { @class = "form-control" })
            @Html.ValidationMessageFor(c => c.Branch.ShortName)
        </div>
        
        <div class="form-group">
            @Html.LabelFor(c => c.Branch.BranchCode)
            @Html.TextBoxFor(c => c.Branch.BranchCode, new { @class = "form-control", @readonly="readonly"})
            @Html.ValidationMessageFor(c => c.Branch.BranchCode)
        </div>

        <div class="form-group">
            @Html.HiddenFor(c => c.Branch.Id)
            @Html.AntiForgeryToken()
            <button id="saveButton" type="submit" class="btn btn-primary">Save</button>
        </div>

        <div class="@ViewBag.CssClass" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
            <strong>@ViewBag.MessageType</strong>@ViewBag.Message
        </div>
    </div>
}

@section scripts
{
    @Scripts.Render("~/bundles/jqueryval")
    <script src="~/Scripts/FormScript/branch.js"></script>

}


